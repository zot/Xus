#!/bin/sh

echo '[["value", null, false, "'$1'"]]' >&4
read line <&3
if [ "$(echo $line | jshon -e 0 -l)" -gt 4 ]; then
   echo $(echo $line | jshon -e 0 -e 4 -u -p -e 5) | awk '{print $1 " = " $2}'
fi
