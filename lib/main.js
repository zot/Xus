// Generated by CoffeeScript 1.3.3
(function() {
  var Server, exports, parseAddr, path, pfs, run, setup, startWebSocketServer, state, stateFd, usage;

  exports = module.exports = require('./base');

  startWebSocketServer = require('./websocket').startWebSocketServer;

  Server = (exports = require('./peer')).Server;

  pfs = require('./pfs');

  path = require('path');

  usage = function(args) {
    console.log("Usage: node " + args[1] + " name [-p port]");
    return process.exit();
  };

  stateFd = null;

  state = null;

  setup = function(cont) {
    var stateFile, xusDir;
    try {
      xusDir = path.join(process.env.HOME, '.xus');
      stateFile = path.join(xusDir, 'state');
      return pfs.stat(xusDir).fail(function() {
        return pfs.mkdir(xusDir);
      }).then(function() {
        return pfs.open(stateFile, 'a+');
      }).then(function(fd) {
        return pfs.flock((stateFd = fd), 'ex');
      }).then(function() {
        return pfs.readFile(stateFd);
      }).then(function(s) {
        return (cont || function() {})(s);
      }).end();
    } catch (err) {
      return console.log("Error: " + err.stack);
    }
  };

  run = function() {
    return setup(function(s) {
      var args, cmd, host, i, name, port, socketAddr, webSocketAddr, xusServer, _ref;
      socketAddr = null;
      webSocketAddr = null;
      state = (s && JSON.parse(s)) || {
        servers: {}
      };
      i = 2;
      args = process.argv;
      if (i > args.length) {
        usage(args);
      }
      name = args[1];
      if (state.servers[name]) {
        console.log("Error: there is already a server named " + name);
        return process.exit(2);
      } else {
        exports.xusServer = xusServer = new Server();
        cmd = null;
        while (i < args.length) {
          switch (args[i]) {
            case '-s':
              socketAddr = args[++i];
              break;
            case '-w':
              webSocketAddr = args[++i];
              break;
            case '-e':
              require(args[++i]).main();
              break;
            case '-x':
              cmd = args[++i];
              break;
            case '-v':
              xusServer.verbose = true;
          }
          i++;
        }
        _ref = parseAddr(webSocketAddr || ':'), host = _ref[0], port = _ref[1];
        return startWebSocketServer(xusServer, host, port, function() {
          console.log("Server " + name + " started on port: " + (xusServer.webSocketServer.address().port));
          process.env.XUS_SERVER = name;
          process.env.XUS_PORT = xusServer.webSocketServer.address().port;
          state.servers[name] = xusServer.webSocketServer.address();
          state.servers[name].pid = process.pid;
          return pfs.truncate(stateFd, 0).then(function() {
            return pfs.writeFile(stateFd, JSON.stringify(state));
          }).then(function() {
            return pfs.close(stateFd);
          }).then(function() {
            if (cmd != null) {
              return require('child_process').spawn('/bin/sh', ['-c', cmd], {
                stdio: ['ignore', 1, 2]
              });
            } else {
              return 1;
            }
          }).end();
        });
      }
    });
  };

  parseAddr = function(addr) {
    var host, parts, port, _ref;
    _ref = parts = addr.split(':'), host = _ref[0], port = _ref[1];
    if (parts.length > 2) {
      throw new Error("Bad address format, expected [host:]port, but got " + addr);
    }
    return (port && [host || null, port]) || [null, host || null];
  };

  exports.run = run;

  exports.setup = setup;

}).call(this);
